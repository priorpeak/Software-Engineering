#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() {
    string command;
    int num1, num2 = 0;

    cout << "Please enter a command code: ";
    cin >> command >> endl;

    while (command != "quit") {
        switch (command) {
            case 'f' || 'F':

        }
    }
}